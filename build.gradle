buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'ru.yandex.qatools.allure:allure-bundle:1.4.19'
        classpath 'ru.yandex.qatools.allure:allure-report:1.3.9'
        classpath 'ru.yandex.qatools.allure:allure-core:1.4.9'
        classpath 'org.codehaus.groovy:groovy-all:2.4.5'
        classpath 'ru.yandex.qatools.clay:clay-aether:2.5'
    }
}
apply plugin: 'groovy'

repositories {
    jcenter()
}

configurations {
    agent
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.5'
    testCompile 'org.spockframework:spock-core:1.0-groovy-2.4'
    testCompile 'junit:junit:4.12'


    compile 'ru.yandex.qatools.allure:allure-spock-1.0-adaptor:1.0'
    agent "org.aspectj:aspectjweaver:1.8.0"
}

test.doFirst {
    jvmArgs "-javaagent:$configurations.agent.singleFile"
}

test.outputs.upToDateWhen {false}

task allureReport << {
    ru.yandex.qatools.allure.AllureMain.main("$buildDir/allure-results", "$buildDir/allure-report")
}